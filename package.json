{
  "name": "@revd/monorepo",
  "private": true,
  "scripts": {
    "reinstall": "rimraf ./node_modules && pnpm install",
    "preinstall": "npx only-allow pnpm"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.8.3"
  }
}
